spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://106.14.168.130/mall_wms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  #nacos注册中心的名字
  application:
    name: mall-ware

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  #主键自增
  global-config:
    db-config:
      id-type: auto
server:
  port: 11000
